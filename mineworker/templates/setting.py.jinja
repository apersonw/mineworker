# 项目配置：只需写要覆盖的项，其余用框架默认值（见 mineworker/setting.py）。
# 运行目录下的 setting.py 会被自动加载；环境变量 MINEWORKER_<KEY> 优先级最高。

# ---- 调度 ----
SPIDER_THREAD_COUNT = 4
SPIDER_MAX_RETRY_TIMES = 3

# ---- 请求 ----
REQUEST_TIMEOUT = 22
RANDOM_USER_AGENT = True

# ---- 数据管道 ----
ITEM_PIPELINES = [
    "mineworker.pipelines.console.ConsolePipeline",
    # "mineworker.pipelines.csv.CsvPipeline",
    # "mineworker.pipelines.mongo.MongoPipeline",
    # "mineworker.pipelines.mysql.MysqlPipeline",
]
ITEM_DEFAULT_TABLE = "items"

# ---- 去重 ----
DEDUP_FILTER = "memory"  # memory（布隆，省内存）| lite（精确 set）

# ---- MongoDB（用到 MongoPipeline 时）----
# MONGO_URI = "mongodb://localhost:27017"
# MONGO_DB = "{{ project_name }}"

# ---- MySQL（用到 MysqlPipeline / create -i --table 时）----
# MYSQL_HOST = "localhost"
# MYSQL_PORT = 3306
# MYSQL_USER = "root"
# MYSQL_PASSWORD = ""
# MYSQL_DB = "{{ project_name }}"

# ---- 浏览器渲染（Request(render=True) 时）----
# WEBDRIVER = dict(pool_size=1, headless=True, load_images=False, render_time=0)

# ---- 日志 ----
LOG_LEVEL = "INFO"
# LOG_FILE = "log/spider.log"

# ---- 告警（阶段 06）----
# WARNING_FEISHU_WEBHOOK = ""
